class component_name_shadow extends HTMLElement {
    constructor(){
        super()
    }
    connectedCallback(){
        shadowRoot = this.attachShadow({mode: 'open'})
        shadowRoot.appendChild(component_name)
    }
}

customElements.define("component_name-shadow", component_name_shadow)